#!/usr/bin/nft -f
# Managed by privatestack-ansible (roles/lab_isolation).
#
# NEVER 'flush ruleset' here: it would wipe the NAT rules libvirt keeps at
# runtime, and they only come back with a libvirtd restart.
# Every brick drops its own table file into /etc/nftables.d/.

include "/etc/nftables.d/*.nft"
