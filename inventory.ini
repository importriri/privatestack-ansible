[hypervisor]
localhost ansible_connection=local

# Service VMs join here as their bricks land (stage A7+), e.g.:
# [services]
# svc-jellyfin ansible_host=10.10.5.10

# Dev workstation VMs (Dev domain, 10.10.3.0/24) join here for
# playbooks/desktop.yml and playbooks/dev.yml:
[workstations]
# devbox ansible_host=10.10.3.10
